Azure backend deployment is deprecated and intentionally disabled.

Do not rename this file back to .yml or .yaml unless Azure deployment is being
restored deliberately. Backend deployment server details are stored locally in:

  backend/.env

Relevant local keys:

  DEPLOY_SERVER_HOST
  DEPLOY_SERVER_USER
  DEPLOY_SERVER_PASSWORD
